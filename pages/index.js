import Layout from './general/layout'

const Index = (props) => {
    return (
        <Layout 
            content={(
                <div>Home Page</div>
            )}
        />
    )
}

export default Index